---
phase: 06
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/home.html
  - src/about.html
  - src/contact.html
  - src/products/blue-diode-laser.html
  - src/products/ultrasonic-surgery.html
  - src/products/holmium-laser.html
  - src/products/consumables.html
autonomous: true
requirements:
  - XCT-01
  - XCT-03
  - XCT-05
must_haves:
  truths:
    - "Page content fades in on load via .page-enter class on <main>"
    - "All decorative icon fonts are hidden from screen readers"
    - "All <img> elements have descriptive alt text or alt=''"
  artifacts:
    - path: "src/home.html"
      provides: ".page-enter class, aria-hidden icons, verified alt text"
      contains: 'class="page-enter" on <main>, aria-hidden="true" on icons'
    - path: "src/about.html"
      provides: ".page-enter class, aria-hidden icons, verified alt text"
      contains: 'class="page-enter" on <main>, aria-hidden="true" on icons'
    - path: "src/contact.html"
      provides: ".page-enter class, aria-hidden icons"
      contains: 'class="page-enter" on <main>, aria-hidden="true" on icons'
    - path: "src/products/blue-diode-laser.html"
      provides: ".page-enter class, aria-hidden icons, verified alt text"
      contains: 'class="page-enter" on <main>, aria-hidden="true" on icons'
    - path: "src/products/ultrasonic-surgery.html"
      provides: ".page-enter class, aria-hidden icons, verified alt text"
      contains: 'class="page-enter" on <main>, aria-hidden="true" on icons'
    - path: "src/products/holmium-laser.html"
      provides: ".page-enter class, aria-hidden icons, verified alt text"
      contains: 'class="page-enter" on <main>, aria-hidden="true" on icons'
    - path: "src/products/consumables.html"
      provides: ".page-enter class, aria-hidden icons, verified alt text"
      contains: 'class="page-enter" on <main>, aria-hidden="true" on icons'
  key_links:
    - from: "<main class='page-enter'>"
      to: "src/css/main.css .page-enter animation"
      via: "CSS class binding"
      pattern: "page-enter"
    - from: "<span class='material-symbols-outlined' aria-hidden='true'>"
      to: "Screen reader accessibility"
      via: "aria-hidden attribute"
      pattern: "aria-hidden=.true.*material-symbols|material-symbols.*aria-hidden"
---

<objective>
Wire up page transitions on all 7 HTML pages by adding .page-enter class to <main> elements, add aria-hidden="true" to all Material Symbols icons for accessibility, and verify all image alt text is present and descriptive.

Purpose: Complete XCT-03 (page transitions wiring), XCT-05 (image alt text + icon accessibility), and XCT-01 (responsive readiness via touch target verification).
Output: All 7 pages with .page-enter class, aria-hidden on icons, verified alt text.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-launch/06-UI-SPEC.md
@src/css/main.css
@src/home.html
@src/about.html
@src/contact.html
@src/products/blue-diode-laser.html
</context>

<interfaces>
<!-- Current <main> element (all 7 pages): -->
```html
<main id="main-content" class="pt-20">
```
Needs to become:
```html
<main id="main-content" class="pt-20 page-enter">
```

<!-- Current Material Symbols pattern (all pages): -->
```html
<span class="material-symbols-outlined text-[28px]">menu</span>
```
Needs to become:
```html
<span class="material-symbols-outlined text-[28px]" aria-hidden="true">menu</span>
```

<!-- Current <img> pattern (product pages): -->
```html
<img src="/images/..." width="..." height="..." alt="Descriptive text" class="...">
```
Already has alt text — verify completeness, no changes needed for product hero images.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add .page-enter class to <main> on all 7 pages</name>
  <files>src/home.html, src/about.html, src/contact.html, src/products/blue-diode-laser.html, src/products/ultrasonic-surgery.html, src/products/holmium-laser.html, src/products/consumables.html</files>
  <read_first>
    - src/home.html (line 45 — current <main> element)
    - src/about.html (line 44 — current <main> element)
    - src/contact.html (line 44 — current <main> element)
    - src/products/blue-diode-laser.html (line 44 — current <main> element)
  </read_first>
  <action>
On ALL 7 HTML pages, find the `<main id="main-content" class="pt-20">` element and add `page-enter` to its class list.

Change from:
```html
<main id="main-content" class="pt-20">
```
To:
```html
<main id="main-content" class="pt-20 page-enter">
```

This applies to:
1. src/home.html
2. src/about.html
3. src/contact.html
4. src/products/blue-diode-laser.html
5. src/products/ultrasonic-surgery.html
6. src/products/holmium-laser.html
7. src/products/consumables.html

Use a global find-replace or individual edits. The class value is identical on all 7 pages.
  </action>
  <verify>
    <automated>grep -l 'class="pt-20 page-enter"' src/home.html src/about.html src/contact.html src/products/*.html | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All 7 HTML files contain `<main id="main-content" class="pt-20 page-enter">`
    - grep count returns exactly 7 files
    - npm run build succeeds
  </acceptance_criteria>
  <done>All 7 pages have .page-enter class on <main>, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Add aria-hidden="true" to all Material Symbols icons</name>
  <files>src/home.html, src/about.html, src/contact.html, src/products/blue-diode-laser.html, src/products/ultrasonic-surgery.html, src/products/holmium-laser.html, src/products/consumables.html</files>
  <read_first>
    - src/home.html (grep for material-symbols-outlined to see all instances)
    - src/products/blue-diode-laser.html (grep for material-symbols-outlined to see all instances)
  </read_first>
  <action>
On ALL 7 HTML pages, find every `<span class="material-symbols-outlined` element that does NOT already have `aria-hidden="true"` and add it.

The pattern to find: `<span class="material-symbols-outlined` (without aria-hidden)
The replacement: `<span class="material-symbols-outlined" aria-hidden="true"`

Use a regex-based find-and-replace across all 7 files:
- Find: `<span class="material-symbols-outlined` (when NOT followed by `aria-hidden`)
- Replace with: `<span class="material-symbols-outlined" aria-hidden="true"`

This affects approximately 154 icon instances across all pages including:
- Hamburger menu icon (all pages)
- Why Choose Us section icons (home.html)
- Product card icons (home.html)
- Product page section icons
- Related product card icons
- Contact page icons
- Footer icons
- Floating WhatsApp button icon

Verify after replacement that NO `<span class="material-symbols-outlined` exists without `aria-hidden="true"`.
  </action>
  <verify>
    <automated>grep -rn '<span class="material-symbols-outlined' src/ | grep -v 'aria-hidden' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep returns 0 for material-symbols-outlined without aria-hidden
    - All 7 files have aria-hidden="true" on every material-symbols-outlined span
    - npm run build succeeds
  </acceptance_criteria>
  <done>All Material Symbols icons have aria-hidden="true", screen readers skip them, build passes</done>
</task>

<task type="auto">
  <name>Task 3: Verify and fix image alt text across all pages</name>
  <files>src/home.html, src/about.html, src/products/blue-diode-laser.html, src/products/ultrasonic-surgery.html, src/products/holmium-laser.html, src/products/consumables.html</files>
  <read_first>
    - src/home.html (all <img> elements — verify alt text)
    - src/about.html (all <img> elements — verify alt text)
    - src/products/blue-diode-laser.html (all <img> elements — verify alt text)
  </read_first>
  <action>
Step 1: Audit all <img> elements across all 7 pages.

Run: `grep -rn '<img ' src/ --include='*.html'`

For each <img> found:
- If it has `alt="..."` with descriptive text → OK, no change needed
- If it has `alt=""` → verify it's truly decorative, OK
- If it has NO alt attribute → add descriptive alt text

Based on prior audit:
- home.html: 5 images, all have alt text ✓
- about.html: 1 image, has alt text ✓
- contact.html: 0 images ✓
- All 4 product pages: hero image + 3 related product card images, all have alt text ✓

Step 2: If any <img> is found without alt text, add descriptive alt following this pattern:
`{product/subject name} — {brief descriptive context relevant to APBA Group's urology business}`

Step 3: Verify no <img> without alt attribute exists:
Run: `grep -rn '<img ' src/ --include='*.html' | grep -v 'alt=' | wc -l`
Must return 0.
  </action>
  <verify>
    <automated>grep -rn '<img ' src/ --include='*.html' | grep -v 'alt=' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep returns 0 for <img> without alt attribute
    - All product images have product-specific descriptive alt text
    - No duplicate alt text for different images on the same page
    - npm run build succeeds
  </acceptance_criteria>
  <done>All <img> elements have alt attributes, no images missing alt text, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML class/attribute changes → Browser rendering | Static HTML, no user input. Safe. |
| aria-hidden → Screen readers | Accessibility attribute, no security impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | HTML page structure | accept | Static files in version control. No runtime modification. |
| T-06-05 | Accessibility | aria-hidden on icons | mitigate | All decorative icons get aria-hidden="true" so screen readers skip them. Verified by grep. |
</threat_model>

<verification>
- All 7 pages have .page-enter class on <main>
- Zero Material Symbols icons without aria-hidden="true"
- Zero <img> elements without alt attribute
- npm run build succeeds
</verification>

<success_criteria>
- Page fade-in animation is wired on all 7 pages via .page-enter class
- All 154+ Material Symbols icons are hidden from screen readers
- All images have descriptive alt text
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch/06-03-SUMMARY.md`
</output>
