---
phase: 06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/css/main.css
autonomous: true
requirements:
  - XCT-01
  - XCT-03
must_haves:
  truths:
    - "Page content fades in smoothly on every page load"
    - "Smooth scroll works for in-page anchor links"
    - "Animations are disabled when prefers-reduced-motion is active"
  artifacts:
    - path: "src/css/main.css"
      provides: "Page transition animation, smooth scroll, reduced motion overrides"
      contains: "pageFadeIn keyframes, scroll-behavior, .page-enter class"
  key_links:
    - from: "src/css/main.css"
      to: "All 7 HTML pages via <main id='main-content'>"
      via: ".page-enter CSS class on <main> element"
      pattern: "pageFadeIn|page-enter|scroll-behavior"
---

<objective>
Add page transition CSS, smooth scroll behavior, and reduced-motion scroll override to the shared stylesheet. This establishes the visual polish foundation that all 7 pages inherit.

Purpose: Implement XCT-03 (page transitions) and XCT-01 responsive scroll behavior per the UI-SPEC Page Transition Contract.
Output: Updated src/css/main.css with 3 new CSS blocks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish-launch/06-UI-SPEC.md
@src/css/main.css
</context>

<interfaces>
<!-- Key CSS contracts the executor needs. Extracted from codebase. -->

From src/css/main.css — existing @theme block (lines 3-83):
- Color tokens: --color-deep-blue, --color-medical-teal, --color-surface, etc.
- Spacing tokens: --spacing-margin-mobile (16px), --spacing-margin-desktop (48px)
- Font tokens: --font-body-md, --font-headline-xl, etc.

From src/css/main.css — existing reduced-motion block (lines 110-115):
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

From all 7 HTML pages — <main> element:
```html
<main id="main-content" class="pt-20">
```
All 7 pages already have this element. The .page-enter class will be added to it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add page fade-in animation and .page-enter class</name>
  <files>src/css/main.css</files>
  <read_first>
    - src/css/main.css (full file — append after existing styles, before the final closing)
  </read_first>
  <action>
Append these CSS rules to src/css/main.css after the existing reduced-motion block (after line 115):

1. Page fade-in keyframes:
```css
@keyframes pageFadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

2. .page-enter class that applies the animation to <main>:
```css
.page-enter {
  opacity: 0;
  animation: pageFadeIn 300ms ease-out forwards;
}
```

3. Reduced-motion override for .page-enter — add inside the existing @media (prefers-reduced-motion: reduce) block (replace the existing block to include this):
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
  .page-enter {
    animation: none !important;
    opacity: 1 !important;
  }
}
```

Do NOT modify any other existing CSS rules. Only append new rules and extend the reduced-motion block.
  </action>
  <verify>
    <automated>grep -c 'pageFadeIn\|page-enter' src/css/main.css</automated>
  </verify>
  <acceptance_criteria>
    - src/css/main.css contains @keyframes pageFadeIn with from/to opacity values
    - src/css/main.css contains .page-enter class with opacity:0 and animation property
    - src/css/main.css reduced-motion block includes .page-enter override with animation:none and opacity:1
    - No existing CSS rules were modified or removed
    - File still has valid CSS syntax (npm run build succeeds)
  </acceptance_criteria>
  <done>Page fade-in animation CSS is in place, reduced-motion override included, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Add smooth scroll and reduced-motion scroll override</name>
  <files>src/css/main.css</files>
  <read_first>
    - src/css/main.css (full file — verify current state after Task 1)
  </read_first>
  <action>
Add these CSS rules to src/css/main.css (append after the Task 1 additions):

1. Global smooth scroll on html element:
```css
html {
  scroll-behavior: smooth;
}
```

2. Reduced-motion scroll override — add this rule inside the existing @media (prefers-reduced-motion: reduce) block:
```css
  html {
    scroll-behavior: auto !important;
  }
```

The final reduced-motion block should look like:
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
  .page-enter {
    animation: none !important;
    opacity: 1 !important;
  }
  html {
    scroll-behavior: auto !important;
  }
}
```

Do NOT modify any other existing CSS rules.
  </action>
  <verify>
    <automated>grep -c 'scroll-behavior' src/css/main.css</automated>
  </verify>
  <acceptance_criteria>
    - src/css/main.css contains `scroll-behavior: smooth` on html selector
    - src/css/main.css reduced-motion block contains `scroll-behavior: auto`
    - npm run build succeeds with no CSS errors
  </acceptance_criteria>
  <done>Smooth scroll enabled globally, disabled for reduced-motion preference, build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS → Browser rendering | Untrusted — CSS is static, no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | src/css/main.css | accept | Static CSS file in version-controlled repo; no runtime modification. Low-value target. |
</threat_model>

<verification>
- npm run build succeeds with no errors
- src/css/main.css contains all 3 new CSS blocks: pageFadeIn keyframes, .page-enter class, scroll-behavior rules
- Reduced-motion block contains all 3 overrides: animation-duration, .page-enter, scroll-behavior
</verification>

<success_criteria>
- CSS file has page fade-in animation (300ms ease-out)
- CSS file has smooth scroll behavior on html element
- Reduced-motion preference disables both animation and smooth scroll
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish-launch/06-01-SUMMARY.md`
</output>
