---
phase: "02"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/partials/header.html
  - src/js/nav.js
  - src/css/main.css
  - src/home.html
  - src/about.html
  - src/contact.html
  - src/products/blue-diode-laser.html
  - src/products/ultrasonic-surgery.html
  - src/products/holmium-laser.html
  - src/products/consumables.html
autonomous: true
requirements:
  - BLD-05
  - BLD-07
  - XCT-02
user_setup: []

must_haves:
  truths:
    - "Header appears on all 7 pages with logo, nav links, CTA button, and hamburger"
    - "Mobile hamburger menu opens/closes with smooth animation and locks body scroll"
    - "Nav links use correct Indonesian copy and point to correct URLs"
  artifacts:
    - path: "src/partials/header.html"
      provides: "Shared header markup with skip link, logo, nav links, CTA, hamburger"
      min_lines: 40
    - path: "src/js/nav.js"
      provides: "Hamburger toggle, scroll lock, mobile menu animation logic"
      exports: ["DOMContentLoaded event listener"]
    - path: "src/css/main.css"
      provides: "Header/nav CSS classes, mobile menu grid animation, reduced motion media query"
      contains: "@media (prefers-reduced-motion"
  key_links:
    - from: "src/js/nav.js"
      to: "src/partials/header.html"
      via: "DOM element selectors (hamburger-btn, mobile-menu, menu-icon)"
      pattern: "getElementById.*hamburger-btn|mobile-menu"
    - from: "src/partials/header.html"
      to: "src/css/main.css"
      via: "Tailwind utility classes + custom CSS for grid animation"
      pattern: "grid-template-rows|mobile-menu"

---

<objective>
Build the shared header/navigation component with desktop nav, mobile hamburger menu, and supporting JavaScript for toggle behavior and scroll lock.

Purpose: Establish the primary navigation pattern that appears identically across all 7 pages per UI-SPEC.
Output: header.html partial, nav.js, updated CSS, header injected into all 7 pages.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shared-layout-navigation/02-CONTEXT.md
@.planning/phases/02-shared-layout-navigation/02-UI-SPEC.md
@src/css/main.css
@vite.config.js

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/css/main.css @theme block:
- Color tokens: --color-deep-blue (#003366), --color-medical-teal (#00A3AD), --color-surface (#f9f9ff), --color-on-surface (#111c2d), --color-border-light (#E2E8F0)
- Spacing tokens: --spacing-gutter (24px), --spacing-margin-desktop (48px), --spacing-margin-mobile (16px), --spacing-stack-sm (8px), --spacing-stack-md (16px), --spacing-stack-lg (32px)
- Font tokens: --font-headline-md (Plus Jakarta Sans), --font-label-md (Inter), --font-body-md (Inter), --font-caption (Inter)

From 02-UI-SPEC.md:
- Header: fixed, top-0, z-50, h-20 (80px), bg-surface/90, backdrop-blur-md, border-b border-border-light
- Skip link: sr-only until :focus-visible, z-[60], bg-deep-blue, text-white
- Nav links: font-label-md (14px, 600), text-on-surface, hover:text-medical-teal
- CTA: bg-deep-blue, text-white, rounded-full, px-6 py-2.5, hover:bg-medical-teal
- Hamburger: p-2 (44x44px min), text-deep-blue, hover:text-medical-teal
- Mobile menu: fixed top-[80px], z-40, bg-surface, grid-template-rows transition 0fr↔1fr
- Active state: text-medical-teal, font-bold (700), border-b-2 border-medical-teal
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create header HTML partial with skip link, logo, nav links, CTA, and hamburger</name>
  <files>src/partials/header.html</files>
  <read_first>
    - src/css/main.css (design tokens)
    - .planning/phases/02-shared-layout-navigation/02-UI-SPEC.md (full header spec lines 152-247)
    - .planning/phases/02-shared-layout-navigation/02-CONTEXT.md (decisions D-02, D-03, D-04, D-08)
  </read_first>
  <action>
Create src/partials/header.html with the complete header markup per UI-SPEC:

1. Skip link: `<a href="#main-content" class="skip-link">Langsung ke konten utama</a>` — positioned above header, sr-only until :focus-visible

2. Header element: `<header class="fixed top-0 w-full z-50 h-20 bg-surface/90 backdrop-blur-md border-b border-border-light shadow-sm">`

3. Nav container inside header: `<nav class="max-w-[--spacing-container-max] mx-auto px-[--spacing-margin-desktop] md:px-[--spacing-margin-desktop] flex justify-between items-center h-full">`

4. Logo: `<a href="/" class="font-headline-md font-bold text-deep-blue hover:text-medical-teal transition-colors">APBA Group</a>`

5. Desktop nav links container: `<div class="hidden md:flex items-center gap-[--spacing-gutter]">` with 4 links:
   - `<a href="/" class="nav-link font-label-md text-on-surface hover:text-medical-teal transition-colors duration-200 py-2">Beranda</a>`
   - `<a href="/products/blue-diode-laser" class="nav-link font-label-md text-on-surface hover:text-medical-teal transition-colors duration-200 py-2">Produk</a>`
   - `<a href="/about" class="nav-link font-label-md text-on-surface hover:text-medical-teal transition-colors duration-200 py-2">Tentang Kami</a>`
   - `<a href="/contact" class="nav-link font-label-md text-on-surface hover:text-medical-teal transition-colors duration-200 py-2">Kontak</a>`

6. CTA button: `<a href="/contact" class="hidden md:inline-flex font-label-md bg-deep-blue text-white rounded-full px-6 py-2.5 hover:bg-medical-teal transition-colors duration-200">Minta Penawaran</a>`

7. Hamburger button: `<button id="hamburger-btn" class="md:hidden p-2 text-deep-blue hover:text-medical-teal" aria-label="Buka menu navigasi" aria-expanded="false" aria-controls="mobile-menu"><span id="menu-icon" class="material-symbols-outlined text-[28px]">menu</span></button>`

8. Mobile menu container (inside header, after nav container):
```html
<div id="mobile-menu" class="md:hidden fixed top-[80px] left-0 w-full z-40 bg-surface border-b border-border-light shadow-lg" style="grid-template-rows: 0fr; opacity: 0; transition: grid-template-rows 300ms ease-in-out, opacity 300ms ease-in-out;">
  <div class="overflow-hidden">
    <div class="flex flex-col">
      <a href="/" class="mobile-menu-item px-[--spacing-margin-mobile] py-[--spacing-stack-md] font-label-md text-on-surface hover:text-medical-teal border-b border-border-light min-h-[44px]">Beranda</a>
      <a href="/products/blue-diode-laser" class="mobile-menu-item px-[--spacing-margin-mobile] py-[--spacing-stack-md] font-label-md text-on-surface hover:text-medical-teal border-b border-border-light min-h-[44px]">Produk</a>
      <a href="/about" class="mobile-menu-item px-[--spacing-margin-mobile] py-[--spacing-stack-md] font-label-md text-on-surface hover:text-medical-teal border-b border-border-light min-h-[44px]">Tentang Kami</a>
      <a href="/contact" class="mobile-menu-item px-[--spacing-margin-mobile] py-[--spacing-stack-md] font-label-md text-on-surface hover:text-medical-teal border-b border-border-light min-h-[44px]">Kontak</a>
      <a href="/contact" class="mobile-menu-cta mx-[--spacing-margin-mobile] my-[--spacing-stack-md] block text-center bg-deep-blue text-white rounded-full px-6 py-3 font-label-md min-h-[44px]">Minta Penawaran</a>
    </div>
  </div>
</div>
```

9. Add CSS for skip-link focus-visible state and active nav-link state to src/css/main.css:
```css
/* Skip link */
.skip-link {
  @apply sr-only;
}
.skip-link:focus-visible {
  @apply not-sr-only block bg-deep-blue text-white font-label-md px-6 py-3 text-center w-full absolute top-0 left-0 z-[60] transition-all duration-200;
}

/* Active nav link state */
.nav-link.active {
  @apply text-medical-teal font-bold border-b-2 border-medical-teal pb-1;
}

/* Mobile menu open state */
#mobile-menu.open {
  grid-template-rows: 1fr !important;
  opacity: 1 !important;
}

/* Mobile menu item active state */
.mobile-menu-item.active {
  @apply text-medical-teal font-bold bg-medical-teal/5 border-l-4 border-medical-teal;
}

/* Reduced motion */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

10. Add Google Fonts links to the `<head>` section of header.html (these will be copied into each page's head):
```html
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Plus+Jakarta+Sans:wght@600;700&display=swap" rel="stylesheet"/>
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>
```
  </action>
  <acceptance_criteria>
    - src/partials/header.html exists and contains `<header>` element with `fixed top-0 w-full z-50`
    - Skip link present with text "Langsung ke konten utama" and `href="#main-content"`
    - 4 nav links present: Beranda, Produk, Tentang Kami, Kontak with correct hrefs
    - CTA button present with text "Minta Penawaran" and `href="/contact"`
    - Hamburger button has `id="hamburger-btn"`, `aria-label="Buka menu navigasi"`, `aria-expanded="false"`, `aria-controls="mobile-menu"`
    - Mobile menu has `id="mobile-menu"` with grid-template-rows transition
    - src/css/main.css contains `.skip-link:focus-visible`, `.nav-link.active`, `#mobile-menu.open`, `@media (prefers-reduced-motion`
    - Google Fonts links present for Inter, Plus Jakarta Sans, and Material Symbols
  </acceptance_criteria>
  <verify>
    <automated>grep -l "hamburger-btn" src/partials/header.html && grep -l "Langsung ke konten utama" src/partials/header.html && grep -l "prefers-reduced-motion" src/css/main.css</automated>
  </verify>
  <done>Header partial created with skip link, logo, 4 nav links, CTA button, hamburger, mobile menu container, and supporting CSS in main.css</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create nav.js with hamburger toggle, scroll lock, and mobile menu animation</name>
  <files>src/js/nav.js</files>
  <read_first>
    - src/partials/header.html (DOM structure created in Task 1)
    - .planning/phases/02-shared-layout-navigation/02-UI-SPEC.md (mobile menu spec lines 250-318, animation spec lines 476-488)
    - .planning/phases/02-shared-layout-navigation/02-CONTEXT.md (decision D-05)
  </read_first>
  <action>
Create src/js/nav.js with the following functionality:

1. DOMContentLoaded event listener wrapping all logic

2. Hamburger toggle:
```javascript
const hamburger = document.getElementById('hamburger-btn');
const mobileMenu = document.getElementById('mobile-menu');
const menuIcon = document.getElementById('menu-icon');

if (hamburger && mobileMenu) {
  hamburger.addEventListener('click', () => {
    const isOpen = mobileMenu.classList.toggle('open');
    hamburger.setAttribute('aria-expanded', isOpen);
    menuIcon.textContent = isOpen ? 'close' : 'menu';
    document.body.style.overflow = isOpen ? 'hidden' : '';
  });
}
```

3. Close menu on nav link click (mobile menu items):
```javascript
const mobileMenuItems = document.querySelectorAll('.mobile-menu-item, .mobile-menu-cta');
mobileMenuItems.forEach(item => {
  item.addEventListener('click', () => {
    mobileMenu.classList.remove('open');
    hamburger.setAttribute('aria-expanded', 'false');
    menuIcon.textContent = 'menu';
    document.body.style.overflow = '';
  });
});
```

4. Close menu on Escape key:
```javascript
document.addEventListener('keydown', (e) => {
  if (e.key === 'Escape' && mobileMenu.classList.contains('open')) {
    mobileMenu.classList.remove('open');
    hamburger.setAttribute('aria-expanded', 'false');
    menuIcon.textContent = 'menu';
    document.body.style.overflow = '';
    hamburger.focus();
  }
});
```

5. Close menu on click outside:
```javascript
document.addEventListener('click', (e) => {
  if (mobileMenu.classList.contains('open') && !mobileMenu.contains(e.target) && !hamburger.contains(e.target)) {
    mobileMenu.classList.remove('open');
    hamburger.setAttribute('aria-expanded', 'false');
    menuIcon.textContent = 'menu';
    document.body.style.overflow = '';
  }
});
```

6. Active state detection:
```javascript
const currentPath = window.location.pathname;
const navLinks = document.querySelectorAll('.nav-link, .mobile-menu-item');
navLinks.forEach(link => {
  const linkHref = link.getAttribute('href');
  if (linkHref === currentPath) {
    link.classList.add('active');
  }
  // Product pages: activate "Produk" link when on any /products/* page
  if (currentPath.startsWith('/products/') && linkHref === '/products/blue-diode-laser') {
    link.classList.add('active');
  }
  // Home page: activate "Beranda" only on exact /
  if (currentPath === '/' && linkHref === '/') {
    link.classList.add('active');
  }
});
```
  </action>
  <acceptance_criteria>
    - src/js/nav.js exists and is wrapped in DOMContentLoaded listener
    - Contains `getElementById('hamburger-btn')` and `getElementById('mobile-menu')`
    - Hamburger click toggles `.open` class on mobileMenu
    - Sets `aria-expanded` on hamburger button
    - Swaps menuIcon text between 'menu' and 'close'
    - Sets `document.body.style.overflow = 'hidden'` when open, `''` when closed
    - Escape key closes menu and focuses hamburger
    - Click outside menu closes it
    - Active state detection compares `window.location.pathname` with link href
    - Product pages activate "Produk" link via `startsWith('/products/')`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "hamburger-btn" src/js/nav.js && grep -c "mobile-menu" src/js/nav.js && grep -c "document.body.style.overflow" src/js/nav.js && grep -c "window.location.pathname" src/js/nav.js</automated>
  </verify>
  <done>nav.js created with hamburger toggle, scroll lock, escape/click-outside close, and active state detection</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Inject header into all 7 pages with proper structure and nav.js script tag</name>
  <files>
    - src/home.html
    - src/about.html
    - src/contact.html
    - src/products/blue-diode-laser.html
    - src/products/ultrasonic-surgery.html
    - src/products/holmium-laser.html
    - src/products/consumables.html
  </files>
  <read_first>
    - src/partials/header.html (header markup to inject)
    - src/js/nav.js (script to reference)
    - src/home.html (existing page structure)
    - vite.config.js (confirms all 7 page entry points)
  </read_first>
  <action>
For each of the 7 HTML files, replace the existing `<body>` content with the header + main structure:

1. Copy the full content of src/partials/header.html into each page's `<head>` and `<body>`:
   - Google Fonts `<link>` tags go in `<head>` after the main.css link
   - Skip link goes as first element in `<body>`
   - Full `<header>` element goes after skip link

2. Add `<main id="main-content" class="pt-20">` after the header — this provides the 80px padding to account for the fixed header. Wrap existing page content inside `<main>`.

3. Add `<script type="module" src="/src/js/nav.js"></script>` before `</body>` in every page.

4. Ensure every page has `<html lang="id">` (already present).

5. Each page structure should look like:
```html
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>[Page Title] — APBA Group</title>
  <link rel="stylesheet" href="/src/css/main.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Plus+Jakarta+Sans:wght@600;700&display=swap" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>
</head>
<body class="bg-surface text-on-surface font-body-md">
  <!-- skip link from header.html -->
  <a href="#main-content" class="skip-link">Langsung ke konten utama</a>
  
  <!-- header from header.html -->
  <header class="fixed top-0 w-full z-50 ...">
    ...
  </header>
  
  <!-- main content with pt-20 for fixed header offset -->
  <main id="main-content" class="pt-20">
    [existing page content]
  </main>
  
  <script type="module" src="/src/js/nav.js"></script>
</body>
</html>
```

6. Verify all 7 pages have identical header markup (only the `<title>` and `<main>` content differ).
  </action>
  <acceptance_criteria>
    - All 7 HTML files contain `<header>` element with `fixed top-0 w-full z-50`
    - All 7 HTML files contain skip link with text "Langsung ke konten utama"
    - All 7 HTML files contain `<main id="main-content" class="pt-20">`
    - All 7 HTML files contain `<script type="module" src="/src/js/nav.js"></script>` before `</body>`
    - All 7 HTML files have Google Fonts links in `<head>`
    - All 7 HTML files have `<html lang="id">`
    - Header markup is identical across all 7 pages (diff check)
  </acceptance_criteria>
  <verify>
    <automated>for f in src/home.html src/about.html src/contact.html src/products/*.html; do grep -q "hamburger-btn" "$f" && grep -q "main-content" "$f" && grep -q "nav.js" "$f" && grep -q "pt-20" "$f" || exit 1; done && echo "All 7 pages have header + nav.js"</automated>
  </verify>
  <done>Header, skip link, main wrapper, and nav.js script injected into all 7 pages with identical markup</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side JS → DOM | nav.js manipulates DOM elements; no external input crosses this boundary |
| Google Fonts CDN → Page | External font resources loaded via `<link>`; no user data sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | nav.js DOM manipulation | accept | Static site with no user input in nav.js; DOM selectors target known IDs only. No external data flows into the script. |
| T-02-02 | Information Disclosure | Google Fonts CDN | accept | Font loading reveals user IP to Google fonts.googleapis.com. Acceptable for corporate website — standard industry practice. No PII transmitted. |
| T-02-03 | Denial of Service | External font CDN | transfer | If Google Fonts CDN is unavailable, fonts fail to load gracefully (fallback to sans-serif). No site breakage. Risk transferred to CDN provider. |
</threat_model>

<verification>
- Build succeeds: `npm run build` produces 7 HTML files
- All 7 pages render with visible header containing logo, nav links, CTA, and hamburger
- Mobile hamburger menu opens/closes with animation
- Body scroll is locked when mobile menu is open
- Skip link visible on Tab key press
- No JavaScript errors in browser console
</verification>

<success_criteria>
- Header appears identically on all 7 pages with correct Indonesian copy
- Mobile hamburger menu toggles open/closed with grid-template-rows animation
- Body scroll locked when menu open, restored when closed
- Escape key and click-outside close the mobile menu
- nav.js loaded as module on every page
- All pages have pt-20 padding on main element for fixed header offset
- Reduced motion preference respected
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-layout-navigation/02-01-SUMMARY.md`
</output>
