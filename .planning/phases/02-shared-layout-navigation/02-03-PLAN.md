---
phase: "02"
plan: 3
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - tests/navigation.test.js
  - tests/accessibility.test.js
autonomous: true
requirements:
  - BLD-05
  - BLD-06
  - BLD-07
  - XCT-02
user_setup: []

must_haves:
  truths:
    - "Navigation links are identical across all 7 pages"
    - "Footer content is identical across all 7 pages"
    - "Mobile menu does not break page scroll when closed"
    - "Active state correctly highlights current page"
  artifacts:
    - path: "tests/navigation.test.js"
      provides: "Tests for nav.js functionality: hamburger toggle, scroll lock, active state"
      min_lines: 30
    - path: "tests/accessibility.test.js"
      provides: "Tests for skip link, ARIA attributes, semantic HTML"
      min_lines: 20
  key_links:
    - from: "tests/navigation.test.js"
      to: "src/js/nav.js"
      via: "DOM event simulation"
      pattern: "hamburger|mobile-menu|active"
    - from: "tests/accessibility.test.js"
      to: "src/*.html"
      via: "HTML parsing for semantic elements and ARIA"
      pattern: "aria-|skip-link|<main|<header|<footer"

---

<objective>
Create automated tests for navigation functionality (hamburger toggle, scroll lock, active state) and accessibility compliance (skip link, ARIA, semantic HTML).

Purpose: Verify BLD-05, BLD-06, BLD-07, XCT-02 requirements are met with automated checks.
Output: navigation.test.js, accessibility.test.js
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-shared-layout-navigation/02-CONTEXT.md
@.planning/phases/02-shared-layout-navigation/02-UI-SPEC.md
@src/js/nav.js (from Plan 01)
@src/partials/header.html (from Plan 01)
@src/partials/footer.html (from Plan 02)
@vitest.config.js
@tests/setup.test.js (existing test patterns)

<interfaces>
From vitest.config.js:
- Test framework: Vitest
- Environment: jsdom (for DOM manipulation tests)
- Test files: tests/*.test.js

From existing tests (tests/setup.test.js, tests/tokens.test.js):
- Pattern: `import { describe, it, expect } from 'vitest'`
- File reading: `import fs from 'fs'` for HTML content verification
- DOM testing: jsdom environment for event simulation
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create navigation behavior tests for hamburger toggle, scroll lock, and active state</name>
  <files>tests/navigation.test.js</files>
  <read_first>
    - src/js/nav.js (implementation to test)
    - src/partials/header.html (DOM structure)
    - vitest.config.js (test configuration)
    - tests/tokens.test.js (existing test patterns)
  </read_first>
  <behavior>
    - Test 1: Hamburger click toggles mobile-menu .open class
    - Test 2: Hamburger click sets aria-expanded to "true" when open, "false" when closed
    - Test 3: Hamburger click swaps menuIcon text between "menu" and "close"
    - Test 4: When mobile menu opens, document.body.style.overflow = "hidden"
    - Test 5: When mobile menu closes, document.body.style.overflow = ""
    - Test 6: Escape key press closes mobile menu when open
    - Test 7: Click outside mobile menu closes it
    - Test 8: Active state detection adds .active class to matching nav link
    - Test 9: On /products/* path, "Produk" link gets .active class
  </behavior>
  <action>
Create tests/navigation.test.js with jsdom environment:

1. Setup: Create DOM elements matching header.html structure (hamburger button, mobile menu, nav links)
2. Import or inline the nav.js logic (use `fs.readFileSync` to load nav.js, then eval in test context, or test the DOM behavior directly)
3. Test hamburger toggle:
   - Simulate click on hamburger button
   - Assert mobileMenu.classList.contains('open') is true
   - Assert hamburger.getAttribute('aria-expanded') === 'true'
   - Assert menuIcon.textContent === 'close'
   - Assert document.body.style.overflow === 'hidden'
   - Click again, assert all reversed
4. Test Escape key:
   - Open menu, dispatch KeyboardEvent('keydown', { key: 'Escape' })
   - Assert menu closed
   - Assert hamburger focused
5. Test click outside:
   - Open menu, dispatch click event on document body (not on menu or hamburger)
   - Assert menu closed
6. Test active state:
   - Set window.location.pathname = '/about'
   - Run active state detection logic
   - Assert "Tentang Kami" link has .active class
   - Set window.location.pathname = '/products/blue-diode-laser'
   - Assert "Produk" link has .active class
  </action>
  <acceptance_criteria>
    - tests/navigation.test.js exists with `import { describe, it, expect } from 'vitest'`
    - Contains test for hamburger click toggling .open class
    - Contains test for aria-expanded attribute changes
    - Contains test for document.body.style.overflow changes
    - Contains test for Escape key closing menu
    - Contains test for click-outside closing menu
    - Contains test for active state detection on /about path
    - Contains test for active state detection on /products/* path
    - All tests pass: `npm test -- tests/navigation.test.js`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/navigation.test.js --run</automated>
  </verify>
  <done>Navigation tests created and passing for hamburger toggle, scroll lock, escape/close-outside, and active state detection</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create accessibility tests for skip link, ARIA attributes, and semantic HTML</name>
  <files>tests/accessibility.test.js</files>
  <read_first>
    - src/partials/header.html (skip link, ARIA attributes)
    - src/partials/footer.html (semantic footer element)
    - src/home.html (full page structure after Plan 01 + 02)
    - .planning/phases/02-shared-layout-navigation/02-CONTEXT.md (decision D-08)
  </read_first>
  <behavior>
    - Test 1: All 7 HTML files contain <header> element
    - Test 2: All 7 HTML files contain <nav> element inside header
    - Test 3: All 7 HTML files contain <main id="main-content"> element
    - Test 4: All 7 HTML files contain <footer> element
    - Test 5: All 7 HTML files contain skip link with href="#main-content"
    - Test 6: All 7 HTML files have <html lang="id">
    - Test 7: Hamburger button has aria-label="Buka menu navigasi"
    - Test 8: Hamburger button has aria-expanded="false" (initial state)
    - Test 9: Hamburger button has aria-controls="mobile-menu"
    - Test 10: Mobile menu has id="mobile-menu"
  </behavior>
  <action>
Create tests/accessibility.test.js:

1. Read all 7 HTML files using fs.readFileSync
2. For each file, parse with a simple regex or DOMParser (jsdom):
   - Assert `<header` present
   - Assert `<nav` present
   - Assert `<main id="main-content"` present
   - Assert `<footer` present
   - Assert `href="#main-content"` present (skip link)
   - Assert `<html lang="id"` present
   - Assert `aria-label="Buka menu navigasi"` present
   - Assert `aria-expanded="false"` present on hamburger
   - Assert `aria-controls="mobile-menu"` present
   - Assert `id="mobile-menu"` present
3. Use `describe.each` to test all 7 files with the same assertions
  </action>
  <acceptance_criteria>
    - tests/accessibility.test.js exists with `import { describe, it, expect } from 'vitest'`
    - Tests all 7 HTML files for <header>, <nav>, <main>, <footer> elements
    - Tests skip link presence with href="#main-content"
    - Tests <html lang="id"> on all pages
    - Tests hamburger ARIA attributes (aria-label, aria-expanded, aria-controls)
    - Tests mobile-menu id presence
    - All tests pass: `npm test -- tests/accessibility.test.js --run`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- tests/accessibility.test.js --run</automated>
  </verify>
  <done>Accessibility tests created and passing for semantic HTML, skip link, lang attribute, and ARIA attributes across all 7 pages</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files → Source files | Tests read source HTML/JS files via fs.readFileSync |
| jsdom → Test assertions | jsdom simulates browser environment; no real browser security boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | Test file system access | accept | Tests only read project source files within the repository. No external network access or sensitive data. |
</threat_model>

<verification>
- All tests pass: `npm test --run`
- Build succeeds: `npm run build`
- Navigation tests cover hamburger toggle, scroll lock, escape/close-outside, active state
- Accessibility tests cover semantic HTML, skip link, lang attribute, ARIA attributes
- Cross-page consistency verified: header/footer markup identical across all 7 pages
</verification>

<success_criteria>
- tests/navigation.test.js exists with at least 8 test cases
- tests/accessibility.test.js exists with at least 10 test cases
- All tests pass with `npm test --run`
- Build produces 7 HTML files without errors
- Header markup identical across all 7 pages (grep diff check)
- Footer markup identical across all 7 pages (grep diff check)
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-layout-navigation/02-03-SUMMARY.md`
</output>
