---
phase: 01-build-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - vite.config.js
  - src/css/main.css
  - src/home.html
  - public/images/
  - tests/images.test.ts
  - tests/tokens.test.ts
autonomous: true
requirements:
  - BLD-02
  - BLD-08
user_setup: []

must_haves:
  truths:
    - "Design tokens from DESIGN.MD render correctly on home page"
    - "Deep blue #003366 color is applied via bg-deep-blue utility class"
    - "Medical teal #00A3AD color is applied via text-medical-teal utility class"
    - "Typography tokens (Plus Jakarta Sans headlines, Inter body) render correctly"
    - "Spacing tokens (section-padding: 80px, container-max: 1280px) work as utilities"
    - "Product images exist in public/images/ and are optimized to WebP with PNG fallbacks"
    - "Images have explicit width and height attributes to prevent layout shift"
    - "Image optimization tests verify WebP conversion and fallback generation"
  artifacts:
    - path: "src/css/main.css"
      provides: "Tailwind v4 @theme directive with all design tokens"
      contains: "@import \"tailwindcss\" and @theme { --color-deep-blue: #003366; --color-medical-teal: #00A3AD; ... }"
    - path: "src/home.html"
      provides: "Design token verification page"
      contains: "Utility classes testing colors, typography, and spacing"
    - path: "public/images/"
      provides: "Optimized product images"
      contains: "WebP format with PNG fallbacks, explicit dimensions"
    - path: "vite-plugin-image-optimizer"
      provides: "Automatic WebP conversion and image optimization"
      contains: "Configuration in vite.config.js for build-time optimization"
    - path: "tests/images.test.ts"
      provides: "Image optimization verification"
      contains: "Tests for WebP format, fallback existence, and dimensions"
    - path: "tests/tokens.test.ts"
      provides: "Design token verification"
      contains: "Tests for color, spacing, and typography tokens"
  key_links:
    - from: "src/home.html"
      to: "src/css/main.css"
      via: "<link> tag referencing compiled CSS"
      pattern: "<link.*rel=\"stylesheet\".*href=\"/assets/.*index.*css\""
    - from: "src/css/main.css"
      to: "DESIGN.MD tokens"
      via: "@theme directive migration"
      pattern: "@theme.*--color-.*--font-.*--spacing-"
    - from: "src/home.html"
      to: "public/images/"
      via: "<img> tags with /images/ paths"
      pattern: "<img.*src=\"/images/.*webp\""
    - from: "vite.config.js"
      to: "public/images/"
      via: "vite-plugin-image-optimizer configuration"
      pattern: "vite-plugin-image-optimizer.*include.*public"
    - from: "tests/images.test.ts"
      to: "public/images/"
      via: "File system checks in tests"
      pattern: "test.*images.*webp.*dimensions"
---

<objective>
Migrate all design tokens from DESIGN.MD Tailwind v3 config to Tailwind v4 @theme directive, verify tokens render correctly, and implement complete image optimization with WebP conversion and PNG fallbacks per BLD-08.

Purpose: Complete the design system foundation so all subsequent phases can use consistent colors, typography, and spacing. Phase 2 (Shared Layout) and Phase 3 (Home Page) depend on these tokens being available. BLD-08 requires WebP with PNG fallbacks for maximum browser compatibility (per RESEARCH.md Open Question #1 resolution).

Output: Working CSS file with 50+ design tokens, test page displaying token samples, optimized images in WebP format with PNG fallbacks, and automated tests for both tokens and images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-foundation/01-RESEARCH.md
@.planning/phases/01-build-foundation/01-UI-SPEC.md
@.planning/phases/01-build-foundation/01-01-PLAN.md (preceding plan: Vite setup)

# Prior Plan Context
@.planning/phases/01-build-foundation/01-01-SUMMARY.md (read after execution)

# Design Token Source
@/home/raihan/Documents/kerja/alkesWeb/DESIGN.MD (lines 13-177: Tailwind v3 config to migrate)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate design tokens from DESIGN.MD to Tailwind v4 @theme directive</name>
  <files>src/css/main.css</files>
  <read_first>
    @/home/raihan/Documents/kerja/alkesWeb/DESIGN.MD (lines 13-177: all color, spacing, font tokens)
    @/home/raihan/Documents/kerja/alkesWeb/.planning/phases/01-build-foundation/01-RESEARCH.md (lines 195-229: Pattern 2)
    @/home/raihan/Documents/kerja/alkesWeb/.planning/phases/01-build-foundation/01-UI-SPEC.md (lines 30-105: spacing and typography specs)
  </read_first>
  <action>
Create src/css/main.css with Tailwind v4 @theme directive containing all 50+ design tokens migrated from DESIGN.MD:

1. Add `@import "tailwindcss";` at the top of the file
2. Create @theme block with CSS custom properties for all tokens from DESIGN.MD:
   - **Color tokens** (migrate all 50+ from lines 18-71): --color-primary: #001e40, --color-deep-blue: #003366, --color-medical-teal: #00A3AD, --color-surface: #f9f9ff, --color-on-surface: #111c2d, --color-background: #f9f9ff, --color-on-background: #111c2d, --color-secondary: #006970, --color-tertiary: #1b1f20, --color-error: #ba1a1a, --color-alert-gold: #FFB800, and all container/variant/fixed colors
   - **Spacing tokens** (lines 78-86): --spacing-section-padding: 80px, --spacing-margin-desktop: 48px, --spacing-margin-mobile: 16px, --spacing-gutter: 24px, --spacing-stack-sm: 8px, --spacing-stack-md: 16px, --spacing-stack-lg: 32px, --spacing-container-max: 1280px
   - **Font families** (lines 88-113): --font-headline-xl: "Plus Jakarta Sans", sans-serif, --font-headline-lg: "Plus Jakarta Sans", sans-serif, --font-headline-md: "Plus Jakarta Sans", sans-serif, --font-body-md: "Inter", sans-serif, --font-label-md: "Inter", sans-serif
   - **Border radius tokens** (lines 72-76): --border-radius-default: 0.125rem, --border-radius-lg: 0.25rem, --border-radius-xl: 0.5rem, --border-radius-full: 0.75rem

3. DO NOT create tailwind.config.js. Tailwind v4 uses CSS-native @theme directive (per RESEARCH.md Pitfall 2).

Use the exact token names and values from DESIGN.MD. The @theme syntax is: `@theme { --color-token-name: #value; }`
  </action>
  <verify>
    <automated>grep -c "@theme" src/css/main.css === 1 && grep -c "import \"tailwindcss\"" src/css/main.css === 1</automated>
  </verify>
  <done>
src/css/main.css exists with @import "tailwindcss" and @theme block containing all 50+ color tokens, 8 spacing tokens, 5 font family tokens, and 4 border radius tokens from DESIGN.MD.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create design token verification test page in home.html</name>
  <files>src/home.html</files>
  <read_first>
    @/home/raihan/Documents/kerja/alkesWeb/.planning/phases/01-build-foundation/01-UI-SPEC.md (lines 30-105: which tokens to test)
    @/home/raihan/Documents/kerja/alkesWeb/src/home.html (existing minimal HTML from Task 1.2)
  </read_first>
  <action>
Update src/home.html to be a design token verification page that tests all migrated tokens:

1. Add `<link rel="stylesheet" href="/assets/main.css">` in <head> to load the compiled Tailwind CSS (Vite outputs to /assets/)
2. In <body>, create sections testing each token category:
   - **Colors**: Create divs with background colors: bg-deep-blue (#003366), bg-medical-teal (#00A3AD), bg-surface (#f9f9ff), bg-error (#ba1a1a), bg-alert-gold (#FFB800). Label each div with the color name and hex code.
   - **Typography**: Create text samples using font families: font-headline-xl (48px Plus Jakarta Sans), font-headline-md (24px Plus Jakarta Sans), font-body-md (16px Inter), font-label-md (14px Inter). Display each with its size and weight.
   - **Spacing**: Create sections demonstrating spacing utilities: py-section-padding (80px), px-margin-desktop (48px), gap-gutter (24px). Use labeled boxes to show the spacing scale.
   - **Border radius**: Create boxes with rounded borders: rounded-default, rounded-lg, rounded-xl, rounded-full. Label each with its radius value.

3. Add headings to organize each test section: "Color Tokens", "Typography Tokens", "Spacing Tokens", "Border Radius Tokens"
4. Use semantic HTML: <section>, <h1>, <h2>, <div>, <span>

This page is for development verification only. It will be replaced with real home page content in Phase 3.
  </action>
  <verify>
    <automated>npm run build && grep -c "bg-deep-blue\|text-medical-teal\|font-headline-xl\|py-section-padding" dist/home.html > 0</automated>
  </verify>
  <done>
home.html displays color swatches, font samples, spacing examples, and border radius boxes. Opening http://localhost:5173/home shows all design tokens rendering correctly with labels.
  </done>
</task>

<task type="auto">
  <name>Task 3: Install and configure vite-plugin-image-optimizer for WebP conversion</name>
  <files>package.json, vite.config.js</files>
  <read_first>
    @/home/raihan/Documents/kerja/alkesWeb/.planning/phases/01-build-foundation/01-RESEARCH.md (lines 43-46, 62-65, 243-247: image optimization requirements and Standard Stack)
  </read_first>
  <action>
Install and configure vite-plugin-image-optimizer to implement BLD-08 WebP conversion with PNG fallbacks (per RESEARCH.md Open Question #1 resolution - use <picture> with WebP source and PNG fallback for maximum compatibility):

1. Install the image optimizer plugin and Sharp dependency:
   - Run `npm install -D vite-plugin-image-optimizer@latest sharp@latest`
   - Verify package.json contains these devDependencies

2. Configure vite.config.js to use the plugin:
   - Import the plugin: `import { viteStaticCopy } from 'vite-plugin-static-copy'` and `import { defineConfig } from 'vite'`
   - Add plugin to plugins array: `viteStaticCopy({ targets: [{ src: 'public/images/*', dest: 'images' }] })`
   - Configure image optimizer with specific settings for WebP generation with PNG fallbacks

3. The plugin will automatically:
   - Convert PNG source images to WebP format at build time
   - Generate PNG fallbacks for older browsers
   - Preserve image dimensions to prevent layout shift (CLSI)
   - Compress images to reduce file size

DO NOT manually convert images or create separate WebP files. The plugin handles this automatically during build (per RESEARCH.md Don't Hand-Roll section).
  </action>
  <verify>
    <automated>grep -E "vite-plugin-image-optimizer|sharp" package.json | wc -l === 2</automated>
  </verify>
  <done>
vite-plugin-image-optimizer and Sharp are installed. vite.config.js includes image optimizer configuration. Build process will automatically generate WebP with PNG fallbacks.
  </done>
</task>

<task type="auto">
  <name>Task 4: Move product images and implement WebP optimization with fallbacks</name>
  <files>public/images/, src/home.html</files>
  <read_first>
    @/home/raihan/Documents/kerja/alkesWeb/.planning/phases/01-build-foundation/01-RESEARCH.md (lines 43-46, 243-247: image optimization requirements)
    @/home/raihan/Documents/kerja/alkesWeb/CLAUDE.md (local images constraint)
  </read_first>
  <action>
Move product images from project root to public/images/ and implement WebP optimization with PNG fallbacks per BLD-08:

1. Create public/images/ directory
2. Move 4 PNG files from project root to public/images/:
   - blue_diode_laser_medical_device_for_urology_clean_white_background_professional.png
   - ultrasonic_surgery_system_medical_equipment_professional_studio_photography.png
   - professional_high_end_medical_holmium_laser_machine_in_a_modern_clean_operating.png
   - modern_medical_corporate_office_interior_with_professional_healthcare_team.png

3. For each image, determine dimensions using `file` command: `file public/images/*.png`
   - Record width and height for each image
   - Create a reference table noting dimensions for use in <img> attributes (per RESEARCH.md Pitfall 2: always specify width/height)

4. Update home.html to include sample images using <picture> element with WebP source and PNG fallback (per RESEARCH.md Open Question #1 resolution):
   ```html
   <picture>
     <source srcset="/images/blue-diode-laser.webp" type="image/webp">
     <img src="/images/blue-diode-laser.png" width="{w}" height="{h}" alt="Blue Diode Laser">
   </picture>
   ```
   - Use explicit width/height attributes to prevent layout shift
   - Add alt text for accessibility
   - Use /images/ paths (no /public/ prefix per RESEARCH.md Pitfall 3)

5. Run `npm run build` to trigger vite-plugin-image-optimizer conversion
6. Verify build output includes both WebP and PNG formats in dist/images/

This implements BLD-08 completely: WebP with PNG fallbacks and explicit dimensions.
  </action>
  <verify>
    <automated>ls -la public/images/*.png | wc -l === 4 && npm run build && ls -la dist/images/*.webp | wc -l === 4</automated>
  </verify>
  <done>
public/images/ contains all 4 product PNG source images. Build process generates WebP versions in dist/images/. home.html includes <picture> elements with WebP source and PNG fallback. All images have explicit width/height attributes to prevent layout shift.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create automated tests for image optimization and design tokens</name>
  <files>tests/images.test.ts, tests/tokens.test.ts</files>
  <read_first>
    @/home/raihan/Documents/kerja/alkesWeb/.planning/phases/01-build-foundation/01-RESEARCH.md (lines 400-427: Validation Architecture)
  </read_first>
  <action>
Create automated tests to verify image optimization and design tokens (per VALIDATION.md Wave 0 requirements):

1. Create tests/images.test.ts with image verification tests:
   - Test that public/images/ contains 4 PNG source images
   - Test that build process generates WebP versions in dist/images/
   - Test that each WebP image has corresponding PNG fallback
   - Test that image files have valid dimensions (non-zero width/height)
   - Use Vitest's describe/test syntax and Node.js fs module for file checks
   - Use Sharp library to read image dimensions: `sharp(imageFile).metadata()`

2. Create tests/tokens.test.ts with design token tests:
   - Test that src/css/main.css exists and contains @theme directive
   - Test that key color tokens are defined (deep-blue, medical-teal, surface)
   - Test that spacing tokens are defined (section-padding, margin-desktop)
   - Test that font family tokens are defined (headline-xl, body-md)
   - Use regex or CSS parsing to verify token presence in compiled CSS

3. These tests address VALIDATION.md Wave 0 requirements for:
   - BLD-02: Design tokens render correctly (token presence tests)
   - BLD-08: Images optimize to WebP with fallbacks (image format and dimension tests)

4. Update package.json if needed: Add `"test:images": "vitest run tests/images.test.ts"` and `"test:tokens": "vitest run tests/tokens.test.ts"`
  </action>
  <verify>
    <automated>test -f tests/images.test.ts && test -f tests/tokens.test.ts && npm run test:run</automated>
  </verify>
  <done>
tests/images.test.ts exists with image optimization tests. tests/tokens.test.ts exists with design token tests. `npm run test:run` executes all tests successfully. BLD-02 and BLD-08 requirements are verified automatically.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS compiler→runtime | @theme directive compiles to CSS utility classes |
| image files→browser | PNG/WebP images served from public/ directory |
| image optimizer→file system | vite-plugin-image-optimizer reads and converts images at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Image file path traversal | mitigate | Images served from public/ at root path. Vite prevents parent directory access. Always use /images/ paths, not ../public/images/ (per RESEARCH.md Pitfall 3). |
| T-01-06 | Information Disclosure | Build output contains CSS with all design tokens | accept | Design tokens are colors and spacing values. No sensitive data. Public website assets. |
| T-01-07 | Denial of Service | Large image files causing slow builds | accept | 4 PNG images at ~3MB total. vite-plugin-image-optimizer will compress and convert to WebP, reducing size. Acceptable for Phase 1. |
| T-01-08 | Tampering | Image optimizer processing malicious files | mitigate | vite-plugin-image-optimizer uses Sharp which validates image formats. Only processes PNG/JPEG from public/images/. No user uploads in Phase 1. |
</threat_model>

<verification>
## Design Token Verification

### Color Tokens
- Open http://localhost:5173/home
- Verify deep-blue (#003366) swatch displays correctly
- Verify medical-teal (#00A3AD) swatch displays correctly
- Verify all 50+ color tokens from DESIGN.MD have corresponding swatches

### Typography Tokens
- Verify Plus Jakarta Sans font loads for headline samples
- Verify Inter font loads for body and label samples
- Verify font sizes match DESIGN.MD specifications (48px, 24px, 16px, 14px)

### Spacing Tokens
- Verify section-padding (80px) creates correct vertical spacing
- Verify margin-desktop (48px) creates correct horizontal spacing
- Verify all 8 spacing tokens from DESIGN.MD work as utilities

### Border Radius Tokens
- Verify rounded-default (0.125rem) creates subtle rounding
- Verify rounded-full (0.75rem) creates fully rounded corners

## Image Verification (BLD-08)

### Build-Time Conversion
- Run `npm run build` to trigger image optimization
- Verify dist/images/ contains 4 WebP files
- Verify dist/images/ contains 4 PNG fallback files
- Verify WebP files are smaller than original PNGs (compression worked)

### HTML Implementation
- Verify home.html uses <picture> elements
- Verify <source> tags reference WebP format: `type="image/webp"`
- Verify <img> tags reference PNG fallbacks
- Verify all <img> tags have explicit width and height attributes

### Browser Compatibility
- Verify WebP images load in modern browsers (96%+ coverage)
- Verify PNG fallbacks exist for older browsers
- Verify no layout shift when images load (CLS check)

## Test Verification (Wave 0)
- Run `npm run test:images` — image tests must pass
- Run `npm run test:tokens` — token tests must pass
- Run `npm run test:run` — all tests must pass
</verification>

<success_criteria>
1. All 50+ color tokens from DESIGN.MD are migrated to @theme directive
2. All 8 spacing tokens from DESIGN.MD are migrated to @theme directive
3. All 5 font family tokens from DESIGN.MD are migrated to @theme directive
4. All 4 border radius tokens from DESIGN.MD are migrated to @theme directive
5. home.html verification page displays all tokens correctly
6. Colors render with correct hex values (deep-blue: #003366, medical-teal: #00A3AD)
7. Typography renders with correct fonts and sizes
8. Spacing utilities create correct pixel values
9. All 4 product PNG source images exist in public/images/
10. Build process generates WebP versions in dist/images/ (BLD-08 satisfied)
11. home.html includes <picture> elements with WebP source and PNG fallback (BLD-08 satisfied)
12. All images have explicit width/height attributes to prevent layout shift (BLD-08 satisfied)
13. tests/images.test.ts verifies WebP conversion and fallback generation
14. tests/tokens.test.ts verifies design token migration
15. No build errors related to CSS compilation or image optimization
</success_criteria>

<output>
Create `.planning/phases/01-build-foundation/01-02-SUMMARY.md` when done
</output>
